# Orders MicroService

---

docker compose up -d

npm run start:dev


---

```
npx prisma migrate dev --name init


```


## quite esto del docker compose  ## 

    networks:
      - custom_network

    networks:
        custom_network:
        driver: bridge
# orders-ms
